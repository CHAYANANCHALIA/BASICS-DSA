import java.util.*;
public class Avg{
    public static float AvgFun(int a,int b,int c){
        float avg = (float)((a+b+c)/3.0);/*Chatgpt-"To ensure that floating-point division is performed, at least one of the operands should be a floating-point number. Hence, you should use 3.0 instead of 3 in the division."
        sidenote-just to verify the above"one of the operands" i changed one argument to float and 3.0 to 3 it still worked*/
        
        
        return avg;/*sidenote return function stops execution of program as soon as func. execution reaches it,nothing after it will matter */
    }
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        int c = sc.nextInt();
        System.out.print(AvgFun(a,b,c));
        
    }
}
